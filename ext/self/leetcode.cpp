/**
 * @author yuzining
 * @date $CURRENT_YEAR/$CURRENT_MONTH/$CURRENT_DATE $CURRENT_HOUR:$CURRENT_MINUTE"
 */
 
#ifdef local
#include "debug.hpp"
using namespace std;
#else
#define DEBUG(...) do { } while(false)
#endif
 
 
 
#ifdef local
int main() {
 
}
#endif